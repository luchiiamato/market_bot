[pytest]
testpaths = tests
pythonpath =
    .
    packages/engine/src
    packages/identity/src
    packages/portfolio/src
    packages/reference_data/src
    services
filterwarnings =
    ignore::DeprecationWarning
asyncio_default_fixture_loop_scope = function
addopts = -ra --strict-markers
